application:
  environment: local

spark:
  master: local[*]
  shuffle_partitions: 8

storage:
  lakehouse_root: ./lakehouse
  checkpoint_root: ./checkpoint
  quarantine_root: ./quarantine

logging:
  level: WARNING
  format: json
  log_directory: ./log
  destination: console
